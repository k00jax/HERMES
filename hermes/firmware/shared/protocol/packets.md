# Hermes Protocol Packets

One packet per line, newline terminated.

SENS,up=<seconds>,rssi=<rssi_or_999>,heap=<bytes>,psram=<bytes>,ct=<temp_c_or_nan>

Example:
SENS,up=27,rssi=999,heap=249304,psram=8373944,ct=35.4
